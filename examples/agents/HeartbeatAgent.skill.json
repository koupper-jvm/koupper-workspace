{
  "name": "HeartbeatAgent",
  "version": "1.0.0",
  "description": "Proactive condition monitor — evaluates ~/.koupper/heartbeat.md conditions and dispatches agents when triggered. Run on a short schedule for autonomous behaviour.",
  "role": "Proactive scheduler / condition evaluator",
  "entrypoint": "setup",
  "inputs": ["~/.koupper/heartbeat.md"],
  "outputs": ["logs/default/heartbeat.log", "job files in configured queues", "~/.koupper/heartbeat-state.json"],
  "envVars": [
    { "name": "CORTEX_JOBS_DIR", "required": false, "description": "Jobs directory (default: ~/.koupper/jobs)" }
  ],
  "providers": [],
  "triggers": ["schedule-rate", "schedule-cron"],
  "tags": ["heartbeat", "proactive", "scheduler", "condition-monitor", "autonomous"],
  "persistent": false,
  "config": {
    "file": "~/.koupper/heartbeat.md",
    "conditions": ["file_exists", "queue_empty", "queue_has_failed", "time_after", "always"],
    "cooldown": "minimum minutes between triggers per condition (default 60)"
  },
  "usage": "koupper schedule add HeartbeatAgent.kts --rate=60000 --id=heartbeat && koupper worker --enable-scheduling",
  "docs": "https://koupper.com/docs/agents/heartbeat"
}
